import actionTypes from '../action-creators/contact/contactActionTypes.js';

const initialState = {

};

const contactReducers = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default contactReducers;
