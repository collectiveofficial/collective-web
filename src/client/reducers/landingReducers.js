import actionTypes from '../action-creators/landing/landingActionTypes.js';

const initialState = {

};

const landingReducers = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default landingReducers;
